pipeline{
    agent any

    stages {
        stage("build"){
            steps{
                 echo 'buidling the app'
            }
        }

         stage("testing"){
            steps{
                 echo 'buidling the app'
            }
        }
         stage("deploy"){
            steps{
                 echo 'buidling the app'
            }
        }
    }
}